name: cyr_flutter_core
description: Shared Flutter core — network, bloc, dependency injection, and presentation helpers.
version: 0.1.0
homepage:

environment:
  sdk: ^3.5.0
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter

  dio: ^5.7.0
  flutter_bloc: ^9.1.1
  get_it: ^8.0.3
  meta: ^1.15.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
